<!--
undone - this query metadata is not being picked up
-->
<query xmlns="http://labkey.org/data/xml/query">
    <metadata>
        <tables xmlns="http://labkey.org/data/xml">
            <table tableName="adapted_numdays" tableDbType="TABLE">
                <tableTitle>Flask Adaptation</tableTitle>
                <columns>
                    <!-- currently not provding the url to get to the flask details -->
                    <column columnName="SampleID">
                        <url>/icemr/showMaterial.view?rowId=${RowId}</url>
                    </column>
                </columns>
            </table>
        </tables>
    </metadata>
</query>

